version: '3.8'

services:
  supermicro-license-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: supermicro-license-generator
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
