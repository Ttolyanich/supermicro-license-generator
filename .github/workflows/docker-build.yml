name: Build Docker Image and Windows Executable

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build-windows:
    name: Build Standalone Windows EXE
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.21'

      - name: Build Windows Executable
        run: |
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o supermicro-license-generator.exe main.go

      - name: Upload Windows Executable Artifact
        uses: actions/upload-artifact@v5
        with:
          name: supermicro-license-generator-windows
          path: supermicro-license-generator.exe

      # Publish a GitHub Release only for tag pushes (e.g. `git tag v1.2.0`),
      # using the pushed tag. Every push to main still builds and uploads the
      # artifact above for validation, but does not overwrite a release.
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Supermicro License Generator ${{ github.ref_name }}
          body: |
            ## ⚡ Supermicro License Generator & SUM Activator ${{ github.ref_name }}

            ### 🖥️ Windows Standalone App (.exe)
            - Download `supermicro-license-generator.exe` below.
            - To use direct SUM activation, place it alongside the
              `sum_2.15.0_Win_x86_64` folder containing `sum.exe`.
            - Double-click `supermicro-license-generator.exe` to run.

            ### 🐳 Docker Container
            `docker pull ghcr.io/ttolyanich/supermicro-license-generator:latest`
          files: supermicro-license-generator.exe

  build-docker:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry (ghcr.io)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase Repository Name
        id: string
        run: |
          echo "REPO_LC=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ env.REPO_LC }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag
            type=sha,format=short

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
